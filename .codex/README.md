## 구조

- `skills/`: 실행 가능한 skill 문서
- `spec/`: spec 산출물과 spec 작성 가이드

## 원칙

- 실제 호출 대상 문서 = `skills` 아래 배치
- 학습 기록, 설계 문서, 실행 문서 = `spec` 아래 배치
- spec 작성 규칙 필요 시 `skills/docs/spec-writing` 참고
- skill 작성 규칙 필요 시 `skills/docs/skill-writing` 참고
